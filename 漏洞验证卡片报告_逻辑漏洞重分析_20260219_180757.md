# 漏洞验证卡片报告（逻辑漏洞重分析）

- 生成时间: 2026-02-19 18:07:57
- 报告范围: 仅静态分析，按你的新约束重跑
- 已排除: `VC-02/15/16/24/25/26`；`VC-27` 不按达标计算
- 中间产物目录: `Tests/temp/msrc_logic_reanalysis_20260219_180757`

## 一、MSRC 标准映射与判定依据

本轮按“标准1”收敛到 4 个高优先维度：
1. 边界跨越: 服务端输入是否能驱动客户端高权限动作。
2. 影响类型: 凭据/权限 > 完整性 > 审计 > 可用性。
3. 利用稳定性: 能否稳定触发，而非一次性异常。
4. 证据闭环: 必须可给出 source->sink->impact 链。

评分（10分）:
- 边界 3 + 影响 3 + 可利用性 2 + 证据 2。
- `>=8.0` 为标准1候选。

## 二、Top 候选与评分表

| ID | 标题 | Source -> Sink（关键路径） | 影响 | 分数 | 当前判断 |
|---|---|---|---|---:|---|
| LV-01 | Cluster Events 自动订阅节点名信任 | `ClusterEventsMonitor.cs:331,441` -> `EventLogSubscription.cs:60` -> `EventLogSession.cs:296` | 可触发对攻击者主机远程事件日志认证（潜在凭据暴露/中继） | 8.1 | 标准1候选 |
| LV-02 | 事件导出 UNC 路径信任 | `ClusterEventsStartPageControl.cs:556-565,603-605,611-613` | 客户端 SMB 外连与文件复制，可能造成认证外泄 | 7.9 | 可升级（接近标准1） |
| LV-03 | 复制组名称进入 WQL 后错对象操作 | `NativeMethods.cs:1207` -> `PResourceType.cs:355-357` -> `CimAdapter.cs:3298,3329,3347,3107,3118,3126` | 错对象复制关系删除/解绑/改配置（高完整性） | 7.8 | 可升级（高价值） |
| LV-04 | Critical Events XPath 语义污染 | `NodeContext.cs:285` / `ResourceContext.cs:538` -> `EventLogFilter.cs:322,325` | 审计误导与告警隐藏 | 7.6 | 可升级 |
| LV-05 | WMI ManagementPath 名称插值错绑 | `WmiAdapter.cs:49,1139,2088,3408` | 错对象加载与管理动作偏移 | 7.2 | 可升级 |
| LV-06 | Network/NI Name-ID 映射漂移 | `ClusApiAdapter.cs:2422-2423,2452,2327,2156-2161,2886-2887,2922,2787,2632-2637` | 错对象操作（confused deputy） | 6.8 | 需动态证据 |
| LV-07 | SyncRename 重名提前返回 | `ObjectManagerHelper.cs:262,268,272,291` | 缓存一致性破坏（链路放大） | 6.3 | 低优先 |

## 三、串联漏洞图（单点未达标但链路达标）

1. `LV-01 + LV-02 + LV-04`
- 节点名信任 -> 强制远程会话/SMB -> 审计语义污染
- 价值: 同时具备“认证面影响 + 检测绕过”。

2. `LV-03 + LV-06`
- 复制组名查询错绑 -> 名称映射漂移放大
- 价值: 提升错对象管理操作稳定性，靠近高影响完整性漏洞。

## 四、可直接提交方向 vs 需后续补强

### A. 可直接冲刺方向（建议优先）
1. `LV-01`：重点证明对非集群真实节点也会发起远程事件日志认证会话。
2. `LV-03`：重点证明复制组名可导致 WQL 错选并执行到错误复制组。

### B. 需后续动态证据
1. `LV-02`：抓取 SMB 认证与文件写入行为证据。
2. `LV-04`：构造可重复“漏报关键事件”样本。
3. `LV-05/LV-06`：证明错对象管理动作的可见副作用。

## 五、结论与停止原因

1. 按你当前排除条件，结果仍未达到“10个标准1达标项”。
2. 但本轮已经把主线从 DoS 转向 3 条高价值逻辑链：
- 强制远程认证链（LV-01/LV-02）
- 复制组错绑执行链（LV-03）
- 审计绕过链（LV-04）
3. 本轮停止原因: 重新分析阶段已完成并产出新链路报告；当前未达到10项，后续需动态证据闭环提升接受度。

## 六、中间文件索引

1. `Tests/temp/msrc_logic_reanalysis_20260219_180757/msrc_criteria_matrix.md`
2. `Tests/temp/msrc_logic_reanalysis_20260219_180757/attack_surface_index.csv`
3. `Tests/temp/msrc_logic_reanalysis_20260219_180757/vc_cards_v2.md`
4. `Tests/temp/msrc_logic_reanalysis_20260219_180757/chain_graph.md`
