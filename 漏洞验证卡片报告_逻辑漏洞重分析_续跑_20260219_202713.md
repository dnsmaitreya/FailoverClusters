# FailoverClusters 逻辑漏洞重分析报告（续跑2h）

- 生成时间: 2026-02-19 20:27:13
- 报告范围: 静态分析（不含复验/fuzzing）
- 本轮硬约束:
  - 排除 `VC-02/15/16/24/25/26`
  - `VC-27` 不达标
  - 纯服务器->客户端 DoS 降级，不作为主成果

## 1. MSRC 标准1映射与判定依据

本轮按 4 轴评分（满分 10）：边界跨越(3) + 影响强度(3) + 可利用稳定性(2) + 证据闭环(2)。

- `>=8.0`: 标准1静态候选
- `6.0-7.9`: 可升级候选

核心强调:
1. 优先“强制认证/凭据暴露”与“错对象高权限管理动作”。
2. 仅崩溃类或纯可用性影响不计入主提交方向。

## 2. Top 候选与评分表

| Rank | ID | 候选 | 关键证据 | 评分 | 结论 |
|---:|---|---|---|---:|---|
| 1 | LV-03 | 复制组名WQL错选 -> 高权限错对象动作 | `CimAdapter.cs:3271,3298,3329,3347 -> 3066,3107,3118,3126,3249` | 8.3 | 标准1候选 |
| 2 | LV-01 | EventLog自动远程会话强制认证 | `ClusterEventsMonitor.cs:331,441 -> EventLogSubscription.cs:60 -> EventLogSession.cs:296` | 8.2 | 标准1候选 |
| 3 | LV-05 | FileShare后台CIM强制认证 | `PNetNameResource.cs:117-127,254 -> FileShareDataSourceBase.cs:135 -> CimUtilities.cs:20` | 8.1 | 标准1候选 |
| 4 | LV-14 | VM复制非UI动作目标信任 -> 错主机高权限动作 | `VirtualMachineResource.cs:2683...2946 -> 4015,4026,4034,4062,4068` | 8.0 | 标准1候选 |
| 5 | LV-04 | 复制通知缓存投毒 | `ClusApiAdapter.cs:6912,6921 + PResourceType.cs:350,357,407` | 7.9 | 可升级 |
| 6 | LV-06 | CAP DnsName信任导致管理目标偏转 | `Group.cs:1529,1538 + DnsSupport.cs:13 -> StandAloneDfsGroup.cs:45/DtcGroup.cs:46/MsmqGroup.cs:47` | 7.8 | 可升级 |
| 7 | LV-08 | 事件导出UNC复制链 | `ClusterEventsStartPageControl.cs:537,546,555,558,601,605,609,611` | 7.8 | 可升级 |
| 8 | LV-07 | Share Wizard/PropertyPage目标信任 | `AddShareCommand.cs:211,223 + FileShare.cs:415 + ServerManagerProxy.cs:60,74` | 7.6 | 可升级 |

## 3. 串联漏洞图与“链路达标”说明

### Chain-A（主线）
`LV-04(缓存投毒)` -> `LV-03(WQL错选)` -> `高权限错对象删除/解绑/改配置`

- 单点 `LV-04` 证据偏“放大器”，但与 `LV-03` 串联后形成完整可利用链，达到标准1语义。

### Chain-B（新增重点）
`LV-13(名称-ID漂移)` -> `LV-14(OwnerNode目标信任)` -> `非UI复制动作误导到错误主机`

- 解决了“仅客户端外连”的弱点，提升为“错误目标上的高权限复制控制”。

### Chain-C
`LV-01(EventLog远程会话)` + `LV-08(UNC导出复制)` + `LV-11(XPath污染)`

- 形成“凭据面 + 审计完整性”复合链，优先提交凭据暴露版本。

## 4. 提交分组

### 可直接提交方向（优先）
1. `LV-03`（可带 `LV-04` 作为链路放大证据）
2. `LV-01`（可带 `LV-08` 作为链路补强）
3. `LV-05`（自动后台认证价值高）
4. `LV-14 + LV-13`（作为“错目标高权限动作”组合包）

### 需后续动态证据方向
1. `LV-06/LV-07`：补充目标偏转成功率与误操作影响面。
2. `LV-11/LV-12`：补充稳定可控输入与业务影响证明。
3. `LV-15`：补充竞态窗口与可重复性。

## 5. 本轮停止原因

按你的指令执行“再运行2小时继续分析”后收敛输出，本轮在续跑窗口结束后停止并出具报告。

---

## 中间产物索引

- `temp/msrc_logic_reanalysis_20260219_202713/msrc_criteria_matrix.md`
- `temp/msrc_logic_reanalysis_20260219_202713/attack_surface_index.csv`
- `temp/msrc_logic_reanalysis_20260219_202713/vc_cards_v2.md`
- `temp/msrc_logic_reanalysis_20260219_202713/chain_graph.md`
